<vte:UserControlExtension x:Class="Translator.Controls.BusyIndicator"
                          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                          xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                          xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                          xmlns:local="clr-namespace:Translator.Controls"
                          xmlns:vte="clr-namespace:VTEControls.WPF;assembly=VTEControls"
                          mc:Ignorable="d"
                          Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}, FallbackValue=collapsed}"
                          d:DesignHeight="450"
                          d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Border Background="Black"
                Opacity=".7" />
        <StackPanel VerticalAlignment="Center">
            <Grid Visibility="{Binding IsMarquee, Converter={StaticResource BoolToVis}}">
                <TextBlock Text="{Binding BusyText, TargetNullValue='Please Wait...'}"
                           FontSize="40"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center" />
            </Grid>

            <Grid Visibility="{Binding IsMarquee, Converter={StaticResource InverseBoolToVis}}">
                <TextBlock FontSize="40"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center">
                    <TextBlock.Text>
                        <MultiBinding StringFormat="{}{0} {1}/{2}">
                            <Binding Path="BusyText"
                                     TargetNullValue="Please Wait..." />
                            <Binding Path="CurrentProgress" />
                            <Binding Path="MaxValue" />
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
            </Grid>
            <ProgressBar IsIndeterminate="{Binding IsMarquee}"
                         Margin="250,20,250,0"
                         VerticalAlignment="Center"
                         Foreground="{DynamicResource Brush.Accent}"
                         Height="25"
                         Maximum="{Binding MaxValue}"
                         Value="{Binding CurrentProgress}"
                         Orientation="Horizontal" />

            <Grid Grid.Row="1"
                  Margin="0,20,0,0"
                  HorizontalAlignment="Center"
                  Visibility="{Binding ShowAbortButton, Converter={StaticResource BoolToVis}, UpdateSourceTrigger=PropertyChanged}">
                <Button Style="{StaticResource AutoSizedButton}"
                        Content="ABORT"
                        FontWeight="DemiBold"
                        Height="30"
                        Width="100"
                        Command="{Binding AbortCommand}" />
            </Grid>
        </StackPanel>
    </Grid>
</vte:UserControlExtension>
