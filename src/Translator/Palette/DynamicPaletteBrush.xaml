<controls:UserControlExtension x:Class="Translator.Palette.DynamicPaletteBrush"
                               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                               xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                               xmlns:controls="clr-namespace:VTEControls.WPF;assembly=VTEControls"
                               xmlns:toolkit="clr-namespace:Xceed.Wpf.Toolkit;assembly=Xceed.Wpf.Toolkit"
                               xmlns:local="clr-namespace:Translator.Palette"
                               mc:Ignorable="d"
                               Background="White"
                               BorderBrush="DimGray"
                               BorderThickness="1"
                               Margin="2">
    <UserControl.Resources>
        <Style x:Key="EmptyButtonStyle"
               TargetType="{x:Type Control}">
            <Setter Property="Padding"
                    Value="1" />
            <Setter Property="Background"
                    Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="content_border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="25">
                            <ContentPresenter x:Name="content"
                                              Margin="{TemplateBinding Padding}"
                                              RecognizesAccessKey="True"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter TargetName="content_border"
                                        Property="BorderBrush"
                                        Value="#FF3C7FB1" />
                                <Setter TargetName="content_border"
                                        Property="BorderThickness"
                                        Value="2" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid Margin="2">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="225" />
            <ColumnDefinition Width="50" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Border Grid.Column="0"
                Background="{DynamicResource Brush.Secondary}">
            <TextBlock Text="{Binding BrushName}"
                       VerticalAlignment="Center"
                       Margin="4,0,0,0"
                       Foreground="White" />
        </Border>
        <Grid Grid.Column="0"
              Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="BrushValueText"
                     Text="#FFFFFFFF"
                     LostFocus="BrushValueText_LostFocus"
                     VerticalAlignment="Center" />
            <Button Grid.Column="1"
                    Grid.Row="1"
                    ToolTip="Resets the brush to last applied."
                    Background="Transparent"
                    BorderThickness="0"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Right"
                    Command="{Binding ResetBrush}">
                <StackPanel>
                    <Image Source="/Translator;component/Images/Reset16.png"
                           Width="16"
                           Height="16" />
                </StackPanel>
            </Button>
            <Button Grid.Column="2"
                    Grid.Row="1"
                    ToolTip="Resets the brush to default."
                    Background="Transparent"
                    BorderThickness="0"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Right"
                    Command="{Binding DefaultResetBrush}">
                <StackPanel>
                    <Image Source="/Translator;component/Images/ResetAll16.png"
                           Width="16"
                           Height="16" />
                </StackPanel>
            </Button>
        </Grid>
        <Border Width="40"
                Height="40"
                Grid.Column="1"
                Grid.RowSpan="2"
                Background="{Binding Brush, UpdateSourceTrigger=PropertyChanged}"
                CornerRadius="50"
                Margin="4"
                BorderBrush="#FFAEAEAE"
                BorderThickness="1" />
        <Button x:Name="BrushSwatchButton"
                Grid.Column="1"
                Grid.RowSpan="2"
                Width="40"
                Height="40"
                Style="{StaticResource EmptyButtonStyle}"
                Click="BrushSwatchButton_Click" />
        <Popup x:Name="BrushSwatchPopup"
               PlacementTarget="{Binding ElementName=BrushSwatchButton}"
               StaysOpen="False">
            <toolkit:ColorCanvas x:Name="ColorPicker"
                                 SelectedColorChanged="ColorPicker_SelectedColorChanged" />
        </Popup>
    </Grid>
</controls:UserControlExtension>
